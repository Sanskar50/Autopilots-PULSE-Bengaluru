# Firebase Configuration
# For local development, you can use Firebase Admin SDK with Application Default Credentials
# Run: gcloud auth application-default login
# Or set up a service account key and set GOOGLE_APPLICATION_CREDENTIALS

# Alternative: Direct Firebase config (if using service account key file)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/your/service-account-key.json

# Firebase Project ID (optional, can be auto-detected)
# FIREBASE_PROJECT_ID=your-project-id

# Firebase Database URL (used in agent.py)
FIREBASE_DATABASE_URL=https://pulse-bengaluru-2933b-default-rtdb.firebaseio.com/


